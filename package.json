{
  "name": "polaris",
  "version": "0.1.0",
  "private": true,
  "description": "Polaris - god-mode visibility over your home network: device discovery, live status, mDNS names, and ntfy alerts.",
  "license": "MIT",
  "workspaces": ["server", "web"],
  "scripts": {
    "dev": "concurrently -n server,web -c blue,magenta \"npm:dev:server\" \"npm:dev:web\"",
    "dev:server": "npm run dev -w polaris-server",
    "dev:web": "npm run dev -w polaris-web",
    "build": "npm run build -w polaris-server && npm run build -w polaris-web",
    "start": "npm run start -w polaris-server",
    "test": "npm run test -w polaris-server && npm run test -w polaris-web",
    "install:all": "npm install"
  },
  "devDependencies": {
    "concurrently": "^9.1.0"
  }
}
